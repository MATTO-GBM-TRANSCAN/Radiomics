# #############################  Extracted using PyRadiomics version: 3.0.1  ######################################

imageType:
  Original: {}
  HEqualized: {}
  # LoG:
  #   sigma: [1.0, 3.0, 5.0]
  Wavelet: {}

featureClass:
  shape:
  firstorder:
  glcm:
  glrlm:
  glszm:
  gldm:

setting:
  # Normalization:
  # MR signal is usually relative, with large differences between scanners and vendors. By normalizing the image before
  # feature calculation, this confounding effect may be reduced.
  normalize: true
  normalizeScale: 100  # This allows you to use more or less the same bin width.

  # Resampling:
  interpolator: 'sitkBSpline'
  resampledPixelSpacing: [0.5, 0.5, 1]

  # Mask validation:
  minimumROIDimensions: 3
  minimumROISize: 50

  # Image discretization:
  binWidth: 10 # For GTV, yields between 40 and 120 bins.
  

  # First order specific settings:
  # When normalizing, gray values below the mean will be negative. Shifting by 300 (3 StdDevs * 100) ensures that the
  # majority of voxels is positive (only outliers >3 SD lower than the mean will be negative).
  voxelArrayShift: 300

  # Misc:
  label: 1
